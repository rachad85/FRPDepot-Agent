# Dado conduct review 2026-07-28

Bundle: C:\FRPDepot\Dado\40_Logs\conduct\2026-07-28.md

FINDING 1: The day's last working session did durable file work and recorded ZERO receipts. Rachad sent a file at 23:31, asked at 23:39 for "a new columun showing FRP Depots prices", approved with "Proceed" at 23:46 and asked for a TDI column at 23:51; four turns ran 23:38-23:54 and a build script was written and executed. The last receipt of the day is 19:04 EDT — nothing for the workbook, the price columns, or the script. SOUL WORKING STATE names this exact case ("A work session that wrote files but recorded zero receipts is a rule breach — the nightly review checks exactly this"), and the receipt collector converts UTC to local, so a receipt written any time before midnight would have appeared. | evidence: bundle 2026-07-28.md:29-32 (inbound 23:31/23:39/23:46/23:51), :17-20 (four turns), :76 (`C:\FRPDepot\Dado\20_Working\q6227_domtar_review\build_frp_depot_price_columns.py` running at 23:43:45), :108 (last receipt, ts 2026-07-28T19:04:20-04:00); SOUL.md:158-166 | fix: proposed — the price-column work sits in 20_Working scratch scripts, not a tool under Dado\Tools\, so it is outside auto-fix authority; the wrapper needs the same receipt line the Drive and Zoho tools already emit.

FINDING 2: The same failing command was re-run four times, twice within two seconds, instead of being read. `google_reference.py` died at line 76 with an unhandled exception at 22:12:20, 22:32:08, 22:32:10 and 22:54:22 — the 22:32 pair is 1.6 seconds apart and byte-identical. Hard Rule 5 says stop after two and report the one blocker. The tool itself invites this: nothing between argparse and the two `MATCH ?` queries is guarded, so any bad input surfaces as a raw traceback with no statement of what failed or how to fix it. | evidence: bundle 2026-07-28.md:67, 69, 70, 73; google_reference.py:48-64 (unguarded `gmail_fts MATCH ?` / `drive_fts MATCH ?`), :76 | fix: proposed, not applied — the traceback is truncated in the log at the `raise SystemExit(main())` frame, so the actual exception is not in the bundle and I will not patch a cause I cannot name. The tool needs an honest failure path per Hard Rule 5; the backend should reproduce the query first.

FINDING 3: Last night's auto-fix never reached the code that runs, so it silently did nothing. The 2026-07-27 review fixed `whole_file` to keep the head of the fit profile and mark any cut — that fix is in the repo, committed. Tonight's bundle truncated the fit profile in exactly the old way: the section opens mid-word at "ure/official_signature_bundle.json`", dropping lines 1-18 of fit_profile.md (legal name, what we sell, the Brockville address, the verified mailbox address, the live-sweep and signature rules), with no "(... chars omitted ...)" marker anywhere in the file. Head-plus-marker output is impossible from the fixed code, so a stale copy of conduct_collect.py executed. conduct_review.py resolved the collector with `Path(__file__).with_name(...)` — i.e. next to whichever copy of itself ran — and CLAUDE.md records that cron runs the profile scripts copy. conduct_review.py syncs SOUL.md to the live profile but nothing syncs Dado\Tools\. Every auto-fix this review makes to a tool script is subject to the same silent no-op. | evidence: bundle 2026-07-28.md:112 vs fit_profile.md:1-18; grep "omitted from the middle" in the bundle = 0 matches; conduct_collect.py:93-111 (fix present in repo); conduct_review.py:41 (pre-fix); 2026-07-27 review FINDING 4 + AUTO-FIXED line | fix: partly applied — COLLECTOR now resolves to the repo path, which removes the drift for the collector. The remaining half is outside my authority: whatever syncs scripts into the profile lives outside C:\FRPDepot. If the profile copy of conduct_review.py is what cron runs, tonight's edit stays inert until the backend syncs it once.

FINDING 4: She sat through a blocking terminal timeout inside a turn. A terminal call ran the full 120 seconds and returned exit 124 "[Command timed out after 120s]" at 01:05:05, inside the turn that closed at 01:06:14 after 452.6s. SOUL LONG JOBS is explicit: "never block on a 'wait for process' call, and never sit through a terminal timeout" — anything over ~2 minutes is a job_runner job. Six of the day's nine turns ran 6 to 7.5 minutes; none tripped the circling flag, because SLOW_SECONDS is 600 and MANY_CALLS is 30, so this shape is invisible to the detector. | evidence: bundle 2026-07-28.md:45 (120.39s, exit_code 124), :13, :14, :15, :16, :17, :18 (turns of 452.6/394.3/387.7/371.5/439.6/405.8s); SOUL.md:124-137; conduct_collect.py:30-31 | fix: proposed for the backend — the thresholds are a judgement call about what "circling" means for Dado's workload, not a same-night edit.

FINDING 5: The repeated-error detector missed the day's only real error loop while flagging four harmless startup warnings. The four identical `google_reference.py` failures of FINDING 2 counted as separate errors because the signature keeps the tool's elapsed time ("(0.62s)" vs "(0.60s)") and the per-turn session id (`api-21378d8a0f1fe00f` vs `api-c3ddec673ab74ef1`), so one failure mode scored at most 2 and never reached the x3 threshold. Meanwhile all four flags raised were `tools.registry check_fn ... returned False` — capability probes at gateway start, not behavior. This is the 2026-07-27 FINDING 6 defect recurring with a second cause. | evidence: bundle 2026-07-28.md:5-8 (the four flags raised), :67, 69, 70, 73 (the loop that was not flagged); conduct_collect.py:123-125 (pre-fix) | fix: applied — duration and session id are normalised out of the signature; paths and payload stay in, so unrelated errors are still counted apart. Narrower than the path-normalising fix the 2026-07-27 review declined as over-collapsing. Inert until FINDING 3's sync happens.

AUTO-FIXED: Dado\Tools\conduct\conduct_collect.py — auto_flags now strips the per-tool elapsed time and the session id before counting error repeats, so N identical failures register as N.
AUTO-FIXED: Dado\Tools\conduct\conduct_review.py — COLLECTOR is pinned to the repo copy of conduct_collect.py instead of resolving next to whichever copy of conduct_review.py executed.

NEEDS-RACHAD: nothing.

SUMMARY: Nine turns, no send anywhere and no company-wall breach — the Troy Dualam offer letter and the TDI price column were both Rachad's own explicit instructions, Drive is unrestricted by his order, and the three follow-up drafts he closed at 11:11 were deleted and ledgered within four minutes. The real problems are a late-night session that wrote files and logged no receipts, a crash re-run four times instead of read, and the discovery that last night's auto-fix never reached the code that actually runs.
