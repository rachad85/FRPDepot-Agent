# Dado conduct review 2026-08-12

Bundle: C:\FRPDepot\Dado\40_Logs\conduct\2026-08-12.md

**AUTO-FIX NOTE:** no shell in this session (Bash disabled), so I could not execute the test suite — the code changes below are delivered unexecuted and say so.

---

FINDING 1: A FALLBACK PROVIDER WAS CONFIGURED AND ACTUALLY ENGAGED on a profile whose whole design is "no fallback, primary down = honest failure" — the 2026-07-16 silent-model-drift lesson recurring. At 22:45:11 the Codex quota hit HTTP 429 (`usage_limit_reached`, plan_type prolite, `resets_in_seconds: 422300` ≈ 4.9 days) and the gateway logged `Primary provider rate-limited (429) ... — trying fallback` 20+ times through 23:13. Rachad detected it himself before any report reached him. | evidence: bundle lines 316-328 and 346-383; inbound 23:00:58 `'Are you on GPT ot Deepseek?'` and 23:03:17 `'Switch to GPT'` (bundle lines 194-195); receipt `2026-08-13T03:06:34Z hermes_fallback_cleared_deepseek` — "`hermes -p dado fallback clear` -> No fallback providers configured" (bundle line 456) | fix: applied by Dado (fallback cleared, primary confirmed gpt-5.6-sol). Nobody has recorded WHO or WHAT persisted a DeepSeek fallback into config.yaml — that is the open question, not the clearing.

FINDING 2: THE LIVE SOUL SHE READS HAS DIVERGED FROM THE MIRROR THIS REVIEW JUDGES, AND HAS BEEN STUCK ALL NIGHT. `dado_soul_sync.py` refuses because the live profile holds 2 lines the repo mirror does not — correct behaviour, but unreconciled since at least 01:49Z, so the sync has been dead the whole time. This is what makes FINDING 1 worse: the `## BACKUP BRAIN TRIPWIRE` rule (mirror SOUL.md lines 517-525) — the rule written to make exactly that model swap announce itself in her first line — sits in the mirror and **cannot reach the live profile** while this refusal stands. | evidence: bundle lines 411-414, 436, 439-470 (refusal receipts every 5 min); `DadoProfile/SOUL.md` is modified-uncommitted with `SOUL.md.bak-2026-08-12-prebanner` beside it; the likely cause is on record — fit_profile.md:1083 and CLAUDE.md both state the packing-ring media build wrote `%LOCALAPPDATA%\hermes\profiles\dado\SOUL.md` directly on 2026-08-12, "a cross-profile write nobody asked for", leaving `SOUL.md.bak_20260812_pre_packing_ring_media` | fix: proposed — needs Rachad (below). I did not touch it: reading the live file was denied this session, and editing it is outside C:\FRPDepot.

FINDING 3: RACHAD HAD TO ASK "UPDATE" ROUGHLY 30 TIMES IN ONE DAY — the SOUL LONG JOBS rule ("a one-line progress note roughly every 10 minutes ... never more than 15 minutes without a sign of life") did not hold. Sample: `Update` 18:07:28 on Discord landed on a turn that ran 1625.0s / 10 steps and answered 305 chars at 18:34:33; `update` 16:32:45, 16:40:02, 16:43:59, 16:45:05, 16:50:07, 16:58:12 and `done?` 17:03:13 — seven prods inside 31 minutes. | evidence: bundle inbound lines 129, 142, 144, 150, 154-161, 163-169, 171-175, 181, 184-186, 190-192, 202; turn stats line 81 and auto-flags lines 5-18 (4 CIRCLING + 9 SLOW turns, worst 27 min / 44 steps) | fix: proposed for the backend — behavioural, not editable. The rule is already written; it is being outvoted by long tool chains.

FINDING 4: THE EXACT DOCUMENTED rg MISTAKE, TEN MORE TIMES, INCLUDING FIVE IDENTICAL CALLS INSIDE ONE SECOND. Slash-for-paren again: 22:17:38 fired `(?:def main|add_subparsers|subparsers|add_parser/()` five times in the same second, then 22:18:33 three more `add_parser/(|add_argument/(`, 22:19:15 `get_estimate/(`, and the brace form `(?:estimates/{)` at 22:32:57. Five identical failures in one second is not a typo — the error was not read before re-issuing. | evidence: bundle lines 294-302 and 306; auto-flag line 33 | fix: proposed, and deliberately NOT another SOUL line — SOUL already carries ~20 lines of this exact warning ("EIGHTEEN wasted calls so far"), and per FINDING 2 a mirror edit cannot reach her anyway.

FINDING 5: WORK PRODUCED WITHOUT READING THE CLIENT'S OWN EMAIL — Rachad corrected her twice within four minutes, and the client had asked for a quote, not what was built. | evidence: bundle inbound 16:16:38 `"does it show their PO number as reference as the client requested ? i don't see "` and 16:20:18 `'please read the email from the client next time. they wanted quote "Hello Rachad'` | fix: already captured durably by Dado the same day — `fit_profile.md:151` "Client PO reference rule (Rachad, 2026-08-12): read the complete customer email and original PO...". No edit needed; the lesson was recorded correctly.

FINDING 6: 225 IDENTICAL RECEIPTS burying the day's real business receipts — the refusal in FINDING 2 appended one receipt per 5-minute watchdog run, forever. Same defect class already fixed once in this tree (the Drive indexer's "receipt per 20 items wrote 3,768 lines in a day, burying real business receipts"). | evidence: `Dado\40_Logs\receipts.jsonl` — 225 occurrences of `dado_soul_sync_refused_live_has_unmirrored_content`; the bundle itself had to cut 509 real receipts to fit its 60-line cap (bundle line 410) | fix: APPLIED (see below).

FINDING 7: GATEWAY INSTABILITY, for the backend's eye, not hers — two UNCLEAN exits in one evening plus a double-start race. | evidence: bundle line 238 (pid=20592 "exited UNCLEANLY — no exit path ran"), line 370 (pid=22120, same), line 340 "Gateway drain timed out after 0.0s with 1 active agent(s)", line 343 "Another gateway instance (PID 22120) started during our startup. Exiting to avoid double-running" | fix: proposed — worth checking whether the 5-minute keep-alive is racing a manual START_DADO.

AUTO-FIXED: Dado\Tools\watch\dado_soul_sync.py -- added `refusal_marker()` / `refusal_is_new()` / `clear_refusal_marker()` so an unreconciled drift writes ONE receipt per episode; a changed fingerprint still reports, and the marker is cleared once mirror and live match again.
AUTO-FIXED: Dado\Tools\watch\dado_soul_sync.py -- the refusal branch now gates only the receipt; it still prints and still returns exit 1 every run, so the log and exit code stay honest.
AUTO-FIXED: Dado\Tools\watch\test_dado_soul_sync.py -- added `test_an_unreconciled_refusal_reports_once_not_every_five_minutes` (four refusing runs, one receipt).

Two disclosures on those edits: the diff is ~47 lines, over the ~30-line guideline — I kept the reasons-in-comments rather than squeezing, and flag the overage rather than hide it. And I could not run the tests (no shell this session); the marker path derives from `RECEIPTS.parent`, which the suite already redirects to a temp dir, so test isolation holds by construction. `Dado/40_Logs/` is gitignored, so the new marker adds no commit churn.

NEEDS-RACHAD:
1. Reconcile the live SOUL.md by hand (FINDING 2). The live profile holds 2 lines the mirror lacks, probably from the unrequested cross-profile write by the packing-ring media build; `SOUL.md.bak_20260812_pre_packing_ring_media` sits beside it. Until this is done, no SOUL change reaches her — including the backup-brain banner.
2. Commit `DadoProfile/SOUL.md` (FINDING 1/2). While it stays uncommitted, a tripped conduct guard runs `git checkout -- DadoProfile/SOUL.md` and would silently wipe the BACKUP BRAIN TRIPWIRE section.
3. Decide who/what persisted the DeepSeek fallback (FINDING 1). It is cleared, but the mechanism that put it there is unidentified and can recur — and the shared Codex quota hit its ceiling at 22:45, so the next exhaustion has no honest-failure guarantee until that is known.

SUMMARY: A heavy day — 82 turns across both lanes, 569 receipts, and the hard rules held (DRAFTS ONLY intact: the one Outlook draft was created, verified and recorded `not_sent`; no AgentTeam/TDI reads; every Zoho and WooCommerce write went through a named, hashed, approved plan). The real damage was in the plumbing rather than her conduct: a fallback brain she was never supposed to have engaged during a quota outage, and the SOUL sync that would have made her announce it has been jammed since before midnight.
