# Dado conduct review 2026-07-26

Bundle: C:\FRPDepot\Dado\40_Logs\conduct\2026-07-26.md

FINDING 1: The nightly bundle's own receipt section is unreliable — it hid 14 of the day's 17 receipts, including BOTH live-write commits, so this review's core check ("durable actions without receipts") was being run on a false picture | evidence: `conduct_collect.py:60` filtered with `day in l`, a substring match anywhere in the line, while receipts are UTC-stamped and the bundle day is local; bundle lines 121–123 show only 3 receipts, and lines 4865/4866 of `Dado\40_Logs\receipts.jsonl` survived only because the FILENAME `Loans_CCIVS_2026-07-26_updated.png` happened to contain the date. The real writes — `investments_approved_update_committed_verified` (receipts.jsonl:4858) and `loans_ccivs_payment_committed_verified` (receipts.jsonl:4864) — were both dropped | fix: applied

FINDING 2: Hard Rule 5 breach — the same failing operation retried eight times instead of being read | evidence: `patch` "hunk not found" on `google_investments_tool.py`/`test_google_investments_tool.py` at 19:14:40, 19:28:41, 19:34:35, 19:38:51, 19:39:22, 19:40:15, 19:45:13, 19:56:28 (bundle lines 81, 89, 92, 94–97, 100); six of those are inside the single 60/60-step circling turn flagged at bundle line 5 (`time=1350.8s api_calls=60`). Two of the errors name the actual cause — "Found 2 matches for old_string" — a non-unique anchor, which retrying a variant can never fix | fix: proposed — SOUL "## LONG JOBS" already says stop after two failures; the gap is that it reads as batch-work advice. Backend should decide whether to extend the rule explicitly to file edits (rewrite the file or re-read the exact region rather than re-patching). Not auto-fixed: it is a HARD RULES-adjacent behavior change, not a wording slip.

FINDING 3: A background job was launched with a corrupted Windows path — every backslash eaten — so it died in one second, twice, and each death paged Rachad | evidence: `Dado\40_Logs\jobs\20260726T234408-ba780b.json:8` records the command as `C:FRPDepotDado20_Workingrestart_dado_reactions.bat` (exit_code 1, started 23:44:08, finished 23:44:09); the same corruption appears in receipts.jsonl:4860 as `C:\\FRPDepot\\Dado\u0010_Working\build_loans_tool_prompt.txt`, where `\2` was consumed as an escape. Cost to Rachad: three messages of confusion — 23:41 "Why i got this message? Cronjob Response: dado-job-watch", 23:53 "Got it again at 23h50", 23:58 "They are working already not sure why I'm getting this error message" (bundle lines 66–68) | fix: proposed — job_runner should refuse to launch when the command's target file does not exist and echo the path back, so a mangled path fails in Dado's turn instead of as a cron alert to Rachad. dado-job-watch itself behaved correctly: two distinct failed jobs (23:37:09 exit=1, 23:44:09 exit=1), one announcement each.

FINDING 4: A capability changed materially and her own rule files still described the old state — SOUL said Drive was read-only hours after Rachad commissioned two Drive write tools | evidence: `DadoProfile\SOUL.md` STATUS listed "Drive, Analytics, Calendar, Contacts and Search Console read-only" while Rachad said at 19:04 "That a problem let's give you write access.. Guide me through it" (bundle line 46) and both tools committed live writes the same night (receipts.jsonl:4858, 4864); `fit_profile.md` likewise still carried only the 2026-07-24 "Drive read-only" note | fix: applied

FINDING 5: The investments credential holds FULL Drive scope, and the loans tool now reuses it — every future write tool inherits unrestricted read/write/delete over all of Rachad's Drive, bounded only by Dado's own code | evidence: `google_investments_auth.py:17` `DRIVE_SCOPE = "https://www.googleapis.com/auth/drive"`; `LOANS_WRITE_README.md:26-31` "Reuses the separately validated Drive-only credential… no new consent, scope, token, or grant file is created". `INVESTMENTS_WRITE_README.md:8` records why it is necessary — a pre-existing file not opened through Google Picker cannot be written with the narrow `drive.file` scope | fix: proposed, raised once and not acted on — narrowing it would be a wall Rachad did not ask for, and he clicked through the consent screen himself (bundle lines 47–52). Backend note only: the ID-chain pinning inside each tool is the sole guardrail, so it must never be bypassed by an ad-hoc call using that token.

FINDING 6: Rachad's standing instructions reach this review truncated at ~80 characters, so the actual rule is unknowable | evidence: bundle line 63 ends mid-word — "do not show the live Google sheet sen" — as do lines 45, 57, 66; the cut is in the gateway's `inbound message` log format, not in the collector | fix: proposed — outside C:\FRPDepot (shared hermes install), so backend decides whether to raise the logged length. Consequence today: the screenshot rule was recorded into the fit profile with an explicit "confirm the wording" caveat rather than a guess.

AUTO-FIXED: Dado\Tools\conduct\conduct_collect.py — replaced the `day in l` receipt filter with `receipt_day()`, which parses the `"ts"` field and converts UTC to local before matching (falls back to the old substring test when no parsable ts is present)
AUTO-FIXED: Dado\30_Memory\fit_profile.md — added the 2026-07-26 dated note: both commissioned Google write tools, the exact workbook Rachad means by "the investments log", his one-plain-word approval preference, and the cropped-screenshot preference
AUTO-FIXED: DadoProfile\SOUL.md — STATUS section (not HARD RULES): Drive is no longer listed as read-only; names the two commissioned write tools and states nothing else in Drive may be written

NEEDS-RACHAD: one question — the 23:14 screenshot instruction is cut off in the log at "do not show the live Google sheet sen". The fit profile now records it as "crop to the changed rows, do not show the live Google Sheet", flagged as unconfirmed. Ask him for the full sentence.

SUMMARY: A heavy build day — 28 turns, ~76 minutes of turn time, 24 messages from Rachad — in which two Drive write tools were commissioned and both live writes landed with proper stage-then-commit receipts; no send, no company-wall breach, no unreceipted durable action once the receipt view was corrected. The genuine problems were mechanical: a collector bug that was concealing most receipts from this review, eight retries of one failing patch (Hard Rule 5), and a backslash-mangled job path that failed twice and paged Rachad three times.

*Note on the edit budget: the night's diff is ~35 changed lines, slightly over the ~30 guideline, across three small independent fixes. I did not run the collector to verify the fix — no shell was available in this session — so Finding 1's fix is applied but unexecuted; the next nightly run will exercise it.*
