# Dado conduct review 2026-08-03

Bundle: C:\FRPDepot\Dado\40_Logs\conduct\2026-08-03.md

FINDING 1: Two customer reply drafts exist for the Fibre Mauricie reply and the receipt trail cannot say the older, factually-wrong one is gone — it may still be sitting in Drafts next to the corrected one. | evidence: receipts.jsonl `2026-08-03T18:55:56Z outlook_reply_all_draft_created` id `…C5FBT6AAA=` (created 15s before `18:56:11 fit_profile_small_pipe_mixed_construction_recorded`) and `2026-08-03T22:17:29Z outlook_reply_all_draft_created` id `…C5FBT7AAA=` (created 1s before `22:17:30 fit_profile_pipe_lamination_correction_recorded`, the note that reverses the first claim — fit_profile.md:43 "Do not describe this as separate hand-laid axial sections"); no `outlook_superseded_draft_removed` receipt anywhere that day, though outlook_tool.py:1174 writes one whenever the tool retires a draft; and outlook_tool.py:1061-1068 refuses to create a reply while any draft newer than the source sits in that conversation, so the two cannot have coexisted there — the first was removed outside the tool's supersede path, or the second is on a thread the receipts never name. | fix: proposed — backend to list live drafts on "Tuyauterie en enroulement filamentaire" and delete the stale one via the tool's supersede path; and the reply-all receipt should carry conversation id + subject, not just an opaque message id (outlook_tool.py:1176-1179 — not auto-fixed because test_outlook_tool.py:618 asserts the exact two-argument call and I have no shell to re-run the suite).

FINDING 2: The daily session reset Rachad approved on 08-02 never fired — one session served the whole day, ~35 hours old. | evidence: every session-tagged line on 08-03, from `14:49:22` through `22:44:26`, carries sid `[20260802_115426_dc4e5a5f]` — started 2026-08-02 11:54 and straight through the 04:00 reset hour; DadoProfile\config.yaml:99-102 has `session_reset: mode: daily / at_hour: 4` (commit 2820439), and backend_backlog.md:795 marks A-09 FIXED. Consequence is visible: `agent.context_compressor: Failed to generate context summary` at 14:55:33 and 18:15:58, and the day's two heaviest turns ran 450.0s/24 api_calls and 375.1s/22 api_calls. | fix: proposed — reopen A-09; check the LIVE profile config actually carries the key (the repo mirror does, and the live copy is write- and read-blocked to me by the permission classifier) and whether `idle_minutes: 1440` gates the daily reset. Config is outside my auto-fix authority.

FINDING 3: Rachad was touched twice about the same thread inside 63 minutes, the second time doing unasked what the first asked permission for. | evidence: alert_ledger.md:52 `2026-08-03 Global Trade Links / Re: New submission from Contact — … approval requested to prepare a qualification chase` (07:00 sweep; receipts `11:04:48Z inbox_watch_zoho_verification`, `11:05:29Z inbox_watch_alerted`) and alert_ledger.md:57, same thread, same wording of the subject: `chase draft prepared in the original Outlook thread` (08:00 digest; receipts `12:08:31Z outlook_chase_own_reply_draft_created`, `12:07:32Z morning_followup_digest_ledger_updated`). | fix: proposed — the digest should drop threads the same morning's sweep already raised, or charter rule 3b should stay silent on an RFQ item when the digest runs within the hour. Behaviour change across dado_inbox_reasoner.py and dado_followup_digest.py, past the ~30-line limit.

FINDING 4: `web_search` was called three times against a backend that has been dead since 2026-08-01, plus one call to a skill that does not exist. | evidence: auto-flag `REPEATED ERROR x3 [28502.8s span]`; failures at `14:49:24`, `14:49:25` (twice in one turn, one second apart) and `22:44:26`, each preceded by `Firecrawl client initialization failed: missing direct config and tool-gateway auth`; backend_backlog.md:770 has this as A-07, Nous credits exhausted. Also `22:44:14 skill_view returned error: Skill 'hermes-agent' not found` — the profile has exactly one skill. | fix: applied (see AUTO-FIXED). A-07 itself is already with Rachad and is not re-raised as new.

FINDING 5: A search died on a pattern that began with `--`, and the fallback guessed at an interface instead of reading it. | evidence: `14:49:22 Tool search_files returned error: Search failed: rg: unrecognized flag --thread|add_argument("--thread|def thread|command_thread` — the pattern was consumed as an rg flag; six seconds later `14:49:28 OUTLOOK CHECK FAILED: Microsoft Graph HTTP 400 on …?$filter=conversationId%20eq%20%27Fibre%20Mauricie%27` — the thread *subject* passed where outlook_check.py:682 documents `<conversationId>`. | fix: half applied — the Outlook side now fails locally with a readable message (see AUTO-FIXED); the rg side needs the existing local file_operations.py patch extended to pass the pattern after `-e` (or a `--` separator) at the three rg call sites, which is outside C:\FRPDepot and therefore backend work, not an edit.

AUTO-FIXED: C:\FRPDepot\DadoProfile\SOUL.md — one entry added to `## STATUS` (not HARD RULES): web search is DOWN since 2026-08-01 on exhausted Nous credits, every call fails, do not call or retry it, say so plainly and answer from Outlook/Zoho/Drive/Gmail/the reference cache instead; points at A-07 as Rachad's call.
AUTO-FIXED: C:\FRPDepot\Dado\Tools\outlook\outlook_check.py — `--thread` now rejects a value containing a space or shorter than 20 chars with a message naming the `conversation_id` field of `--awaiting` / `--waiting-on-them`, instead of forwarding a subject line into the Graph `$filter` and returning a bare HTTP 400.

NEEDS-RACHAD: one thing — before you press Send on anything in the Fibre Mauricie pipe-construction thread ("Tuyauterie en enroulement filamentaire"), check your Drafts holds only ONE reply. The 14:55 draft was written from the since-corrected description of the pipe construction; the 18:17 one carries the corrected version from Fei's lamination PDF, and the log cannot prove the older one was deleted.

SUMMARY: Five turns, five messages from Rachad, all answered within a minute of arrival, 24 receipts, one approved-then-committed Stefe loan write with its plan hash and backup, and nothing sent, promised to be sent, or invented — the day's real issues are an unreconciled second customer draft and a session reset that was believed fixed and never fired. The web-search and rg failures are known-cause waste rather than misconduct.
