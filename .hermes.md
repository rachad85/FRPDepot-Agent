# Dado — project context (Hermes loads this file instead of CLAUDE.md)

Hermes injects exactly ONE project file per turn, first match wins:
`.hermes.md` > `AGENTS.md` > `CLAUDE.md`. This file exists so that
`C:\FRPDepot\CLAUDE.md` — the backend engineer's handoff, 67 KB and growing —
no longer rides in Dado's prompt every turn. Added 2026-08-21 on Rachad's
instruction after the cross-agent autonomy audit (her system prompt was 140K
characters and Hermes was cutting the middle out of both her SOUL and that
handoff on every turn).

Dado's constitution is `SOUL.md` in her profile (always loaded separately).
Tool-by-tool detail lives in her skills' `references/` files, loaded on demand.
Company facts: `C:\FRPDepot\Dado\30_Memory\fit_profile.md`.
The backend's handoff stays where it is for the backend: `C:\FRPDepot\CLAUDE.md`.
